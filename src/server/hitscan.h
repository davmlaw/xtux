void hitscan_fire( entity *ent, byte weapon, point weap_start, byte dir );
int hitscan_entity(point A, point B, entity *shooter, netushort *l, int stops);
netushort hitscan_wall(point start, byte dir);
